# NNNN — <작업명> · plan

## 1. 소유 컨텍스트

- **규칙을 소유하는 컨텍스트**: (하나만 고른다)
- **공개 API로 소비하는 컨텍스트**:
- 둘에 걸치면 어느 쪽이 규칙을 소유하고 어느 쪽이 결과를 읽는지 여기서 결정한다. 결정하지 않고 구현을 시작하지 않는다.

## 2. 레이어 배치

`docs/ARCHITECTURE.md`의 의존 방향을 따른다.

| 레이어 | 이 작업에서 바꾸는 것 |
| --- | --- |
| `domain` (규칙·불변 조건) |  |
| `application` (유스케이스, 필요할 때만) |  |
| `infrastructure` (브라우저·외부 기술) |  |
| `presentation` (React·Canvas) |  |
| 공개 API (`index.ts`) |  |

## 3. 설계 판단

되돌리기 어려운 선택과 그 이유만 적는다. 대안을 함께 적는다.

| ID | 판단 | 대안과 기각 이유 | 재검토 시점 |
| --- | --- | --- | --- |
| DEC-01 |  |  |  |

## 4. 검증 계획

- **자동 검증**: 추가·수정할 테스트 파일과 지킬 성질
- **수동 검증**: 사용자 흐름, 화면 폭, 브라우저 조건
- **경계·실패 조건**:
- **호환성**: 저장 형식·DNA·공개 API를 건드리는가. 건드린다면 신구 왕복 테스트가 있는가
- **필수 명령**: `pnpm test`, `pnpm build`

## 5. 요구사항별 추가 확인

`docs/FEATURE_DEVELOPMENT.md`의 표에서 해당하는 줄만 옮겨 적고 실제로 확인한다.

- 

## 6. 위험과 롤백

| 위험 | 영향 | 완화 / 롤백 방법 |
| --- | --- | --- |
|  |  |  |
